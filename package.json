{
  "name": "CodeX",
  "version": "0.0.1",
  "description": "An interpreted, object-oriented and statically typed language.",
  "main": "build/main/run.js",
  "dependencies": {
    "babel-preset-es2015": "^6.1.18",
    "readline-sync": "^1.2.22"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "mocha": "^2.3.4"
  },
  "scripts": {
    "build": "babel src --source-maps --out-dir build",
    "repl": "node build/main/run.js",
    "test": "mocha --recursive build/test/"
  },
  "keywords": [
    "CodeX",
    "parser",
    "tokenizer",
    "type-checking",
    "interpreter"
  ]
}
